{
  "name": "alist-kratos-sdk",
  "version": "1.0.0",
  "description": "JavaScript SDK for AList file API with Ory Kratos authentication",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "dependencies": {
    "@ory/client": "^1.22.37",
    "@ory/integrations": "^1.3.1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "typescript": "^5.7.2"
  }
}
